/*
Program name: style.css
Author: Hamza Muhammad
Date created: 09/22/2025
Date last edited: 09/24/2025
Date submitted: 09/25/2025
Version: 4.0
Description: External CSS stylesheet for patient registration form with custom fonts, colors, and styling
*/

/* Body styling */
body { 
    background-color: lightblue; 
    font-family: Arial, sans-serif;
    color: darkblue;
    margin: 0;
    padding: 20px;
}

/* Header section styling */
.header { 
    background-color: blue; 
    color: white; 
    padding: 15px; 
    text-align: center;
    border-radius: 8px;
    margin-bottom: 20px;
}

/* Header title styling */
h1 {
    color: white;
    font-size: 28px;
    margin: 10px 0;
    font-family: Georgia, serif;
}

/* Main content styling */
.main-content {
    background-color: white;
    padding: 20px;
    border-radius: 8px;
    margin-bottom: 20px;
}

h2 {
    color: darkblue;
    text-align: center;
    font-family: Georgia, serif;
}

/* Logo styling */
.logo { 
    background-color: red; 
    width: 25px; 
    height: 25px; 
    display: inline-block;
    border-radius: 50%;
    margin-right: 10px;
}

/* Table styling */
table {
    width: 100%;
    margin: 20px 0;
    border-collapse: collapse;
}

td {
    padding: 8px;
    vertical-align: top;
}

/* Form input styling */
input[type="text"], 
input[type="email"], 
input[type="password"], 
select, 
textarea {
    padding: 8px;
    border: 2px solid lightgray;
    border-radius: 4px;
    font-family: Arial, sans-serif;
}

input[type="text"]:focus, 
input[type="email"]:focus, 
input[type="password"]:focus, 
select:focus, 
textarea:focus {
    border-color: blue;
    outline: none;
}

/* Checkbox and radio button styling */
input[type="checkbox"], 
input[type="radio"] {
    margin-right: 8px;
}

/* Range slider styling */
input[type="range"] {
    width: 200px;
    margin: 10px 0;
}

/* Button styling */
button {
    padding: 10px 20px;
    margin: 8px;
    border: none;
    border-radius: 5px;
    font-size: 14px;
    cursor: pointer;
    font-family: Arial, sans-serif;
}

button[type="reset"] {
    background-color: red;
    color: white;
}

button[type="submit"] {
    background-color: green;
    color: white;
}

button:hover {
    opacity: 0.8;
}

/* Footer styling */
.footer { 
    background-color: gray; 
    color: white; 
    padding: 15px; 
    text-align: center;
    border-radius: 8px;
}

.footer button {
    background-color: darkblue;
    color: white;
    margin-top: 10px;
}

/* Link styling */
a {
    color: darkblue;
}

a:visited {
    color: purple;
}
